# spinning-frontend

Vite + React frontend for Spinning.

Local dev:

```bash
cd spinning-frontend
npm ci
npm run dev
```

E2E tests (Cypress):

```bash
npm run cy:open
```

Deploy on Vercel: connect this folder as a project, build `npm run build`, output `dist`.
